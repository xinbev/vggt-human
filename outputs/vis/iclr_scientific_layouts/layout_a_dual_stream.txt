LAYOUT A — DUAL-STREAM CONVERGENCE.

Organize the main overview as two narrow parallel scientific streams rather than large illustrations. Upper stream is "Scene stream": RGB thumbnails → VGGT token strip → Camera K / D_vggt / F_scene. Lower stream is "Human stream": RGB thumbnails → NLF detector → metric SMPL parameter strips. The two streams converge diagonally into a compact central block "Metric Human–Scene Coupling" containing analytic coarse gauge and HSI residual affine. From the coupling block, two aligned outputs, Metric depth and Metric SMPL, enter a medium-size TRSTR module containing a small segmented body, a 96-row token matrix, four probe grids, and a weighted voting funnel. Under TRSTR place a narrow temporal strip with nine frames and identity-separated tracks. Final output sits at upper-right as a small scene-grid thumbnail.

Use one large dashed overview boundary on the left 78% and two stacked dashed mechanism insets on the right 22%: inset (b) "Scale Gauge" and inset (c) "Regional + Temporal Refinement". Use curved arrows only at the two stream convergence; all other arrows are straight and precise. Module sizes remain comparable. Caption at bottom-left: "(a) Dual-stream metric reconstruction".

